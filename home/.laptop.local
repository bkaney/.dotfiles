#!/bin/sh

echo "Removing ~/.bin, already using ~/bin for local programs..."
rm -rf "$HOME/.bin"
sed -i bak -e '/export PATH="\$HOME\/.bin:\$PATH"/d' "$HOME/.zshrc"

echo "Removing suspenders gem..."
gem uninstall -aIx suspenders
